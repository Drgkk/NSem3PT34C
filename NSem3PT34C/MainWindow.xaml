<Window x:Class="NSem3PT34.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:NSem3PT34"
        SizeChanged="MainWindow_OnSizeChanged"
        Title="Simple WPF Editor" Height="700" Width="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="_Save" Command="{x:Static ApplicationCommands.Save}" />
                <MenuItem Header="_Load"     Command="{x:Static ApplicationCommands.Open}" />
                <Separator/>
                <MenuItem Header="_End"   Command="{x:Static ApplicationCommands.Close}" />
            </MenuItem>
            <MenuItem Header="_Options">
                <MenuItem Header="Einstellungen..." Name="MenuOptionsSettings" />
            </MenuItem>
        </Menu>

        <ToolBarTray Grid.Row="1">
            <ToolBar>
                <Button x:Name="ToggleBold" Width="28" Height="28" ToolTip="Bold" Click="ToggleBold_Click">B</Button>
                <Button x:Name="ToggleItalic" Width="28" Height="28" ToolTip="Italic" Click="ToggleItalic_Click">I</Button>
                <Button x:Name="ToggleSpellCheck" Width="28" Height="28" ToolTip="Spell Check" Click="ToggleSpellCheck_Click">ABC</Button>
                <Separator/>
                <TextBlock VerticalAlignment="Center" Margin="6,0,2,0">Font:</TextBlock>
                <ComboBox x:Name="FontComboBox" Width="180" SelectionChanged="FontComboBox_SelectionChanged">
                    <ComboBoxItem>Segoe UI</ComboBoxItem>
                    <ComboBoxItem>Calibri</ComboBoxItem>
                    <ComboBoxItem>Times New Roman</ComboBoxItem>
                    <ComboBoxItem>Arial</ComboBoxItem>
                </ComboBox>
                <TextBlock VerticalAlignment="Center" Margin="6,0,2,0">Font Size:</TextBlock>
                <ComboBox x:Name="FontSizeComboBox" Width="50" SelectionChanged="FontSizeComboBox_SelectionChanged">
                    <ComboBoxItem>12</ComboBoxItem>
                    <ComboBoxItem>16</ComboBoxItem>
                    <ComboBoxItem>32</ComboBoxItem>
                    <ComboBoxItem>72</ComboBoxItem>
                </ComboBox>
            </ToolBar>
        </ToolBarTray>

        <Border x:Name="MyCanvasBorder" Grid.Row="2" Margin="8" BorderBrush="LightGray" BorderThickness="1" CornerRadius="4">
            <local:MyCanvas x:Name="DrawingCanvas" MinHeight="600" MinWidth="800"/>
        </Border>
    </Grid>
</Window>
